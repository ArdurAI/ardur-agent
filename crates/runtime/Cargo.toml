[package]
name = "ardur-runtime"
version = "0.0.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
anyhow = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
# The smoke test names `anyhow::Result` + `uuid::Uuid` directly, so they must
# also be available to the integration-test crate.
anyhow = { workspace = true }
uuid = { workspace = true }
