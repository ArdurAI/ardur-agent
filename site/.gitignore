# Node deps for the Tailwind/PostCSS pipeline
node_modules/

# Hugo build artifacts
public/
resources/
.hugo_build.lock

# Generated Tailwind build-stats files
assets/jsconfig.json
hugo_stats.json

# Lockfile is not committed: the deploy uses `npm install` with caret ranges.
package-lock.json
